---
layout:     post
title:      github使用
subtitle:   笔记
date:       2019-8-21
author:     YN
header-img: img/post-bg-ios9-web.jpg
catalog: true
tags:
    - github
    - 笔记
---  


# 建立公钥
``` 
```
```
配置用户名和邮箱：
git config --global user.name "Example Surname"
git config --global user.email "your.email@gmail.com"
# Set default so that all changes are always pushed to the repository
git config --global push.default "matching"
```
```
获取Git配置信息，执行以下命令：
$ git config --list
user.email=motanchenyuan@126.com
user.name=yannian
```
```
上传项目：
（1）输入命令：git remote add origin 仓库地址
（1.1）git pull -f --all（如果没有输入这一行导致GitHub伤的版本里面有readme文件和本地版本冲突，强制覆盖已有分支：git push -u origin master -f）  

（2）输入命令：git push -u origin master

（3）输入命令：git pull  

（4）输入命令：git push
```
